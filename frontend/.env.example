# Read by next.config.js's rewrites() function, which runs in the
# Next.js SERVER (Node process / Vercel serverless function) -- never in
# the browser. The browser only ever calls the same-origin /backend-api/*
# path, which is transparently proxied here.
APP_SERVICE_URL=http://localhost:8000
