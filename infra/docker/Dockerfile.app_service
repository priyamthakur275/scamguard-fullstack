# syntax=docker/dockerfile:1
# Build context: backend/
# Build with:  docker build -f infra/docker/Dockerfile.app_service -t scamguard-app-service backend

FROM python:3.12-slim AS builder

WORKDIR /build
RUN set -eux; \
        apt-get update; \
        for i in 1 2 3 4 5; do \
            apt-get install -y --no-install-recommends build-essential libpq-dev && break || (apt-get update && sleep 2); \
        done; \
        rm -rf /var/lib/apt/lists/*

COPY requirements.txt .
RUN pip install --no-cache-dir --user -r requirements.txt

# --------------------------------------------------------------------------

FROM python:3.12-slim AS runtime

RUN set -eux; \
        apt-get update; \
        for i in 1 2 3 4 5; do \
            apt-get install -y --no-install-recommends libpq5 curl tesseract-ocr libzbar0 && break || (apt-get update && sleep 2); \
        done; \
        rm -rf /var/lib/apt/lists/*; \
        useradd --create-home --uid 1000 appuser

WORKDIR /app

COPY --from=builder /root/.local /home/appuser/.local
COPY app_service ./app_service
COPY alembic ./alembic
COPY alembic.ini .

RUN chown -R appuser:appuser /app
USER appuser

ENV PATH=/home/appuser/.local/bin:$PATH \
    PYTHONUNBUFFERED=1 \
    PYTHONDONTWRITEBYTECODE=1

EXPOSE 8000

HEALTHCHECK --interval=30s --timeout=5s --start-period=15s --retries=3 \
    CMD curl -f http://localhost:8000/api/v1/health || exit 1

# Run migrations, then start the API server. Uses 4 uvicorn workers as a
# production-reasonable default; override via UVICORN_WORKERS if needed.
CMD alembic upgrade head && \
    uvicorn app_service.main:app --host 0.0.0.0 --port 8000 --workers ${UVICORN_WORKERS:-4}
